import Cards from '@/Components/Cards'
import { Color } from '@/utils/colors'
import { NextPage } from 'next'
import Head from 'next/head'
import styled from 'styled-components'
import { Box } from '@/Components/Box'
import Grid from '@/Components/Grid'
import ColumnOne from '@/Components/ColumnOne'
import ColumnTwo from '@/Components/ColumnTwo'
import Spacer from '@/Components/Spacer'
import BottomColumns from '@/Components/BottomColumns'
import { IParallax, Parallax, ParallaxLayer } from '@react-spring/parallax'
import { MutableRefObject, useEffect, useRef, useState } from 'react'
import { useWindowSize } from '@/utils/hooks'
import ContactForm from '@/Components/ContactForm'

const FalseContainer = styled.div`
  font-family: 'Roboto Slab', serif;
  font-size: 4em;
  display: flex;
  justify-content: center;
  align-items: center;
  width: 1000px;
  height: 600px;
  border: 1px solid black;
  border-radius: 8px;

  @media (max-width: 768px) {
    display: flex;
    justifycontent: center;
    alignitems: center;
    font-size: 2.5em;
    width: 400px;
    height: 300px;
  }
`

const Title = styled.h1`
  font-size: 4em;
  color: ${Color.Black};
  @media (max-width: 768px) {
    font-size: 3em;
    width: 400px;
  }
`

const Home: NextPage = () => {
  const title = `Hi, I'm Jake`
  const parallax = useRef<IParallax>(null)
  const windowSize = useWindowSize()

  return (
    <>
      <Head>
        <title>Jake Edgar</title>
        <meta name='description' content='Generated by create next app' />
        <meta name='viewport' content='width=device-width, initial-scale=1' />
      </Head>
      <Parallax ref={parallax} pages={windowSize && windowSize.width && windowSize.width > 768 ? 5 : 6}>
        <ParallaxLayer offset={0} speed={0.25} factor={0.75}>
          <Title>{title}</Title>
          <Box className='card-body'>
            I am a <strong>Senior Full Stack Software Engineer</strong>, with a passion for <strong>bold beautiful designs</strong> and{' '}
            <strong>blazing load speeds</strong>, but my real super power is bringing people together as a team.
          </Box>
        </ParallaxLayer>

        <ParallaxLayer offset={1} speed={0.5} factor={1}>
          <Cards />
        </ParallaxLayer>

        <ParallaxLayer offset={2} speed={0.5} factor={1.25}>
          <Title>My Projects</Title>
          <FalseContainer>Coming Soon...</FalseContainer>
        </ParallaxLayer>

        <ParallaxLayer offset={3} speed={-0.3} factor={1}>
          <Grid columns={2}>
            <ColumnOne />
            <ColumnTwo />
          </Grid>
        </ParallaxLayer>

        <ParallaxLayer offset={4} speed={-0.3} factor={1}>
          <BottomColumns />
        </ParallaxLayer>

        <ParallaxLayer offset={5} speed={1} factor={1}>
          <ContactForm />
        </ParallaxLayer>
      </Parallax>
    </>
  )
}

export default Home
